# Local dev — copy to .env.local
WEB3FORMS_ACCESS_KEY=your_access_key_here

# Optional legacy name (still supported):
# NEXT_PUBLIC_WEB3FORMS_KEY=your_access_key_here

# On Hostinger without hPanel access, add the key to persistent data instead:
# data/apartmentData.json → integrations.web3formsAccessKey
