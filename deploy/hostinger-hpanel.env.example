# Optional — only if you can set hPanel env vars
WEB3FORMS_ACCESS_KEY=your_web3forms_access_key_here

# Preferred on Hostinger without hPanel: add to persistent data/apartmentData.json instead:
# "integrations": { "web3formsAccessKey": "your-key-here" }
